# Scripts
All scripts written in node, which will require less dependencies, instead of mixing with python.


## wave-to-mp3.js
A platform independent node script that converts wav files to mp3.

Prompts for source folder and save files to source/mp3 folder.